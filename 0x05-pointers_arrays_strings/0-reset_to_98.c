#include <stdio.h>
#include <man.h>

/**
 * main - takes a pointer to an int as parameter
 * and updates the value it points to to 98
 * Return: always 0.
 */

int main(void)
{
	int *p = &n;

	n = 98;
	putchar(n);
return (0);
}
